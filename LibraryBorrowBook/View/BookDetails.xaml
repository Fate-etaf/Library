<Window x:Class="LibraryBorrowBook.View.BookDetails"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Book Details &amp; Reviews" Height="550" Width="700"
        Background="#FFFDF5E6" WindowStartupLocation="CenterScreen">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Book Info -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Image Name="imgBook" Width="100" Height="140" Stretch="UniformToFill" HorizontalAlignment="Left" VerticalAlignment="Top"/>
            
            <StackPanel Grid.Column="1" Orientation="Vertical" Margin="10,0,0,0">
                <TextBlock Name="tbTitle" FontSize="20" FontWeight="Bold" Foreground="#FF333333" TextWrapping="Wrap"/>
                <TextBlock Name="tbAuthor" FontSize="14" Foreground="#FF666666" Margin="0,5,0,0"/>
                <TextBlock Name="tbCategory" FontSize="14" Foreground="#FF666666" Margin="0,2,0,0"/>
                <TextBlock Name="tbQuantity" FontSize="14" FontWeight="Bold" Foreground="#FF4CAF50" Margin="0,5,0,0"/>
            </StackPanel>
        </Grid>
        
        <Separator Grid.Row="1" Margin="0,15,0,15"/>

        <!-- Add Review Section -->
        <StackPanel Grid.Row="2" Orientation="Vertical" Margin="0,0,0,15">
            <TextBlock Text="Write a Review" FontWeight="Bold" FontSize="16" Foreground="#FF8C00" Margin="0,0,0,5"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                    <TextBlock Text="Rating (1-5): " VerticalAlignment="Center"/>
                    <ComboBox Name="cbRating" Width="50" SelectedIndex="4">
                        <ComboBoxItem Content="1"/>
                        <ComboBoxItem Content="2"/>
                        <ComboBoxItem Content="3"/>
                        <ComboBoxItem Content="4"/>
                        <ComboBoxItem Content="5"/>
                    </ComboBox>
                </StackPanel>
                
                <TextBox Name="txtComment" Grid.Column="1" Height="25" Margin="0,0,10,0" Padding="5,0,0,0" VerticalContentAlignment="Center" MaxLength="500"/>
                
                <Button Name="btnSubmitReview" Grid.Column="2" Content="Submit Review" Width="100" Height="25" Background="#FF4CAF50" Foreground="White" BorderThickness="0" Click="btnSubmitReview_Click"/>
            </Grid>
        </StackPanel>

        <!-- Reviews List -->
        <ListView Name="lvReviews" Grid.Row="3" BorderBrush="#FFCC80" BorderThickness="1">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical" Margin="5">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding User.UserName}" FontWeight="Bold" Foreground="#FF1976D2"/>
                            <TextBlock Text=" rated " Foreground="Gray"/>
                            <TextBlock Text="{Binding Rating}" FontWeight="Bold" Foreground="#FFFF8F00"/>
                            <TextBlock Text=" Stars on " Foreground="Gray"/>
                            <TextBlock Text="{Binding ReviewDate, StringFormat='dd/MM/yyyy HH:mm'}" Foreground="Gray"/>
                        </StackPanel>
                        <TextBlock Text="{Binding Comment}" TextWrapping="Wrap" Margin="0,5,0,0"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Button Name="btnBack" Grid.Row="4" Content="Back" Width="100" Height="30" HorizontalAlignment="Left" Margin="0,15,0,0" Click="btnBack_Click"/>
    </Grid>
</Window>
